from typing import List, Tuple

def extract_entry_endpoint(user_text: str) -> Tuple[List[float], float]:
    """
    Stub extractor. Implementations MUST be deterministic and logged with:
    extractor_version_id + extractor_config_hash.
    """
    # Replace with your deterministic extractor.
    return [0,0,0,0,0], 0.0

def extract_exit_endpoint(model_text: str) -> Tuple[List[float], float]:
    return [0,0,0,0,0], 0.0
